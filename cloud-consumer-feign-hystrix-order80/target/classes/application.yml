server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka

ribbon:
  ReadTimeout: 3000
  ConnectionTimeout: 3000

#开启feign的hystrix功能
feign:
  hystrix:
    enabled: true
