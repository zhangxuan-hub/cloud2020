server:
  port: 9001
spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
management:
  endpoints:
    web:
      exposure:
        include: "*"
